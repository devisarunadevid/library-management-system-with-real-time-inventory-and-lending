spring.application.name=Librario
spring.datasource.url=jdbc:mysql://localhost:3306/librario_db
spring.datasource.username=root
spring.datasource.password=password@05

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT settings
jwt.secret=MySuperSecretKey123
jwt.expiration=3600000

# B) (Optional) Dev-only: no SMTP, just log to console (use ConsoleMailService below)
# -> comment out the SMTP block above and use the ConsoleMailService implementation.

# Default to console mailer
app.mail.type=${APP_MAIL_TYPE:console}

# (Optional) SMTP defaults — values read from env vars when you enable smtp
# === SMTP (choose ONE) ===
# A) Real Gmail SMTP (requires App Password; not your normal password)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# Razorpay Test Mode Keys
razorpay.key_id=rzp_test_RISuV1xicgfkWf
razorpay.key_secret=gXORpfUlLKJEFpPO4x5eKIBQ
razorpay.webhook_secret=mywebhooksecret123
